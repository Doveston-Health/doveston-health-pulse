import {AppError} from '../../shared/errors/app-error.js';
export const FINANCE_LIMITS=Object.freeze({maxPageSize:100,maxPage:10000,maxRangeDays:730});
export const FINANCE_SIGNALS=Object.freeze({largeOverdueAmount:'5000',largeOverdueDays:30,debtorConcentrationPercent:30,revenueDeclinePercent:15,minimumRevenue:'1000'});
export function pagination(query){const page=Number(query.page||1),pageSize=Number(query.pageSize||25);if(!Number.isInteger(page)||page<1||page>FINANCE_LIMITS.maxPage)throw new AppError('page is invalid.',400);if(!Number.isInteger(pageSize)||pageSize<1||pageSize>FINANCE_LIMITS.maxPageSize)throw new AppError('pageSize must be between 1 and 100.',400);return {page,pageSize,skip:(page-1)*pageSize};}
export function range(query){const end=query.endDate||new Date().toISOString().slice(0,10);const start=query.startDate||`${end.slice(0,8)}01`;for(const [name,value] of [['startDate',start],['endDate',end]])if(!/^\d{4}-\d{2}-\d{2}$/.test(value)||Number.isNaN(Date.parse(`${value}T00:00:00Z`)))throw new AppError(`${name} must use YYYY-MM-DD.`,400);const days=Math.floor((Date.parse(end)-Date.parse(start))/86400000)+1;if(days<1||days>FINANCE_LIMITS.maxRangeDays)throw new AppError(`Date ranges must be between 1 and ${FINANCE_LIMITS.maxRangeDays} days.`,400);return {start:new Date(`${start}T00:00:00Z`),end:new Date(Date.parse(`${end}T00:00:00Z`)+86400000),startDate:start,endDate:end};}
